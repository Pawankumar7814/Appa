//Require module
var User = require("../module/User");
var connectDBDev = require("../config/connection");
var uuid = require("uuid");

// UI refers to UserInformation

class Password {
    async InputUser(UI, cb) {

    }

    async CheckUser()
}